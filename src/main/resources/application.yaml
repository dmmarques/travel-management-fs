spring:
  application:
    name: travel-management-ms
  data:
    mongodb:
      uri: mongodb://mongo:lAdtzaifIvqxhUOleNWUEGeCuegFylXy@yamabiko.proxy.rlwy.net:18303
      database: trips
      #uri: mongodb://localhost:27017/trips

server:
  servlet:
    context-path: /travel-management-ms
  port: 8080
  ssl:
    enabled: false
  management:
    endpoints.web.exposure.include: health, info
    endpoint:
      health:
        probes:
          enabled: true
    server:
      port: 8080
